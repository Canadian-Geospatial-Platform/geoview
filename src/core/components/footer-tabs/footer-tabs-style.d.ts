import { Theme } from '@mui/material/styles';
export declare const getSxClasses: (theme: Theme) => {
    tabsContainer: {
        position: string;
        background: string;
        boxShadow: string;
        width: string;
        transition: string;
        height: string;
    };
};
