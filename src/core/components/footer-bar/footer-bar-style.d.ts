import { Theme } from '@mui/material/styles';
export declare const getSxClasses: (theme: Theme) => {
    tabsContainer: {
        position: string;
        background: string;
        boxShadow: number;
        width: string;
        transition: string;
        height: string;
        '&.MuiGrid-container': {
            background: string;
        };
    };
};
